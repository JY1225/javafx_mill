<?xml version="1.0" encoding="UTF-8"?>

<tables>  	
    <table-entry name="Program" col-data="
	ID INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),
	NAME VARCHAR(100) NOT NULL,
	CREATION TIMESTAMP NOT NULL,
	LASTOPENED TIMESTAMP,
	CONSTRAINT PKEY_PROGRAM PRIMARY KEY (ID)"/>   
	
	<table-entry name="step" col-data="
	ID INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),
	gripper   INTEGER NOT NULL,
	WorkPiece INTEGER NOT NULL,
	UserFrame INTEGER NOT NULL,
	Smooth    INTEGER NOT NULL,
	RobotPutsetting    INTEGER NOT NULL,
	CONSTRAINT PKEY_STEP PRIMARY KEY (ID),
	CONSTRAINT STEP_gripper FOREIGN KEY (gripper)REFERENCES IRSCW.gripper(ID),
	CONSTRAINT STEP_workPiece FOREIGN KEY (workPiece)REFERENCES IRSCW.workPiece(ID),
	CONSTRAINT STEP_userFrame FOREIGN KEY (userFrame)REFERENCES IRSCW.userFrame(ID),
	CONSTRAINT STEP_Smooth FOREIGN KEY (Smooth)REFERENCES IRSCW.Smooth(ID),
	CONSTRAINT STEP_RobotPutsetting FOREIGN KEY (RobotPutsetting)REFERENCES IRSCW.RobotPutsetting(ID)"/>    
	
      <table-entry name="Gripper" col-data="
	ID INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1),
	NAME VARCHAR(100) NOT NULL,
	CREATION TIMESTAMP NOT NULL,
	LASTOPENED TIMESTAMP,
	CONSTRAINT PKEY_PROGRAM PRIMARY KEY (ID)"/>   
</tables>